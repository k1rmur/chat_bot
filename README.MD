# Внутренний телеграм-бот ФАВР

## 📋 Описание

Интеллектуальный Telegram-бот для автоматизации работы с документами и предоставления информации по водным ресурсам России. Бот использует технологии RAG (Retrieval-Augmented Generation) и современные LLM для ответов на вопросы пользователей на основе базы знаний.

### Основные возможности

- 🤖 **Интеллектуальные ответы**: Использование GigaChat-2-Pro для генерации ответов на основе документов
- 📚 **RAG-система**: Поиск релевантной информации в базе знаний с использованием векторных эмбеддингов и BM25
- 🎙️ **Распознавание речи**: Транскрибация аудио/видео сообщений через Salute Speech API
- �️ **Ответы на голосовые сообщения**: Автоматическая транскрибация голосовых сообщений с последующим ответом через RAG-систему
- �📝 **Генерация протоколов**: Автоматическое создание протоколов совещаний из записей
- 📄 **Работа с документами**: Загрузка, обработка и рассылка документов
- 📋 **Суммаризация документов**: Создание кратких содержаний больших документов через команду `/summarize_documents`
- ⏰ **Планировщик задач**: Автоматическая рассылка сообщений по расписанию
- 👥 **Система подписок**: Управление списками рассылки
- 📊 **Статистика**: Логирование действий пользователей и аналитика

## 🛠️ Технологический стек

### Backend & Frameworks
- **Python 3.10**
- **aiogram 3.17.0** - Telegram Bot API фреймворк
- **pyrogram** - альтернативный клиент для работы с Telegram API
- **SQLAlchemy** - ORM для работы с базой данных
- **Alembic** - миграции базы данных
- **APScheduler** - планировщик задач

### AI & ML
- **GigaChat API** - LLM от Сбер (модель GigaChat-2-Pro)
- **LangChain 0.2.16** - фреймворк для работы с LLM
- **LlamaIndex 0.11.19** - индексация и поиск в документах
- **ChromaDB 0.5.15** - векторная база данных
- **HuggingFace Embeddings** - модель `intfloat/multilingual-e5-small` для эмбеддингов

### NLP & Text Processing
- **Whisperx** - распознавание речи
- **Salute Speech** - API распознавания речи от Сбер
- **BM25** - алгоритм ранжирования текстов

### Infrastructure
- **Docker & Docker Compose**
- **PostgreSQL/SQLite** - хранение данных пользователей

## 📂 Структура проекта

```
chat_bot_inner/
├── bot/                          # Основной код бота
│   ├── config_data/              # Конфигурация
│   ├── database/                 # Модели и запросы к БД
│   ├── handlers/                 # Обработчики команд и сообщений
│   │   ├── user_handlers.py      # Обработка пользовательских команд
│   │   ├── send_documents.py     # Рассылка документов
│   │   ├── video_protocols.py    # Обработка аудио/видео
│   │   └── docsummary.py         # Суммаризация документов
│   ├── services/                 # Бизнес-логика
│   │   ├── rag.py                # RAG-система
│   │   ├── prompt_templates.py   # Шаблоны промптов
│   │   ├── converter.py          # Конвертация медиа
│   │   └── map_reduce_*.py       # Обработка больших текстов
│   ├── keyboards/                # Клавиатуры для бота
│   ├── lexicon/                  # Текстовые ресурсы
│   ├── filters/                  # Фильтры сообщений
│   ├── middlewares/              # Middleware (БД, логирование)
│   ├── data/                     # Данные для внутреннего режима
│   ├── data_citizens/            # Данные для граждан
│   ├── db/                       # Векторная БД (inner)
│   ├── db_citizens/              # Векторная БД (outer)
│   ├── get_bot.py                # Точка входа
│   └── make_embeddings.py        # Создание эмбеддингов
├── documents/                    # Документы для обработки
├── alembic/                      # Миграции БД
├── docker-compose.yml            # Docker Compose конфигурация
├── Dockerfile                    # Образ для бота
├── embeddings.yml                # Конфигурация для создания эмбеддингов
├── requirements.txt              # Python зависимости
└── .env_inner                    # Переменные окружения (не в репозитории)
```

## 🚀 Установка и развертывание

### Предварительные требования

- Docker и Docker Compose
- Git
### 1. Клонирование репозитория

```bash
git clone https://github.com/k1rmur/chat_bot.git
cd chat_bot
```

### 2. Настройка переменных окружения

Создайте файл `.env_inner` в корне проекта:

```env
# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token
API_ID=your_telegram_api_id
API_HASH=your_telegram_api_hash

# Database
DB_URL=sqlite+aiosqlite:////user_db/user.db

# GigaChat API
CREDENTIALS=your_gigachat_credentials
CREDENTIALS_NONRAG=your_gigachat_credentials_for_nonrag

# Sber Speech API
SBER_SPEECH_API_KEY=your_sber_speech_api_key

# Application Mode
MODE=inner

# Password
ADD_USER_PASSWORD=пароль_для_включения_в_рассылку
```

### 3. Подготовка данных

Поместите CSV-файлы с данными в соответствующую папкк:
- `bot/data/`

### 4. Создание эмбеддингов

Перед первым запуском необходимо создать векторные индексы:

```bash
# Для Linux/Mac
./make_embeddings.sh

# Для Windows в bash
bash make_embeddings.sh
```

Или через Docker Compose:

```bash
docker-compose -f embeddings.yml up --build
```

### 5. Запуск бота

```bash
docker-compose up --build -d
```

### 6. Проверка работы

```bash
# Просмотр логов
docker-compose logs -f bot-inner

# Проверка статуса контейнера
docker-compose ps
```

## 📝 Использование

### Основные команды бота

- `/start` - Запуск бота и главное меню
- `/help` - Список доступных команд
- `/subscribe` - Подписка на рассылку (требуется пароль)
- `/summarize_documents` - Суммаризация загруженных документов
- `/process` - Обработка загруженных документов для суммаризации
- `/clear` - Очистить состояние и загруженные документы
- `/send_document` - Отправить документ для рассылки (только админы)
- `/check_documents` - Проверить список документов
- `/statistics` - Получить статистику использования (только админы)
- `/get_report` - Получить отчет

### Функциональность

#### 1. Вопросы и ответы
Просто отправьте вопрос боту, и он найдет ответ в базе знаний:
```
Пользователь: Какие реки протекают в Московской области?
Бот: [ответ на основе данных из CSV-файлов]
```

#### 2. Голосовые сообщения с RAG-ответами
Отправьте голосовое сообщение боту:
- Бот автоматически транскрибирует речь с помощью Salute Speech API
- Покажет распознанный текст вопроса
- Найдет ответ в базе знаний через RAG-систему
- Отправит текстовый ответ, как на обычный текстовый вопрос

**Пример использования:**
```
1. Пользователь отправляет голосовое: "Какая организация отвечает за Волгу?"
2. Бот: "Голосовое сообщение получено, готовится ответ..."
3. Бот: "Распознанный вопрос: Какая организация отвечает за Волгу?"
4. Бот: [детальный ответ из базы знаний]
```

#### 3. Обработка аудио/видео для протоколов
Отправьте видео/аудио файл или документ (.txt, .doc):
- Бот распознает речь из медиафайла
- Создаст текстовую транскрипцию
- Сгенерирует структурированный протокол совещания
- Отправит документ с протоколом

**Поддерживаемые форматы:**
- Видео файлы (конвертируются в аудио)
- Аудио файлы
- Текстовые файлы (.txt, .doc)

#### 4. Суммаризация документов
Команда `/summarize_documents` позволяет получить краткое содержание больших документов:

**Процесс использования:**
```
1. Отправьте команду: /summarize_documents
2. Загрузите один или несколько документов
3. После загрузки всех документов отправьте: /process
4. Бот обработает документы и вернет файл с суммаризацией
```

**Возможности:**
- Обработка нескольких документов одновременно
- Использование Map-Reduce подхода для больших текстов
- Генерация структурированного краткого содержания
- Результат в формате .docx

**Очистка состояния:**
- Используйте `/clear` для сброса загруженных документов

#### 5. Рассылка документов
Администраторы могут:
- Загружать документы в `/documents_to_send`
- Настраивать автоматическую рассылку по расписанию
- Отправлять сообщения подписчикам

## 🔧 Конфигурация

### Настройка RAG-системы

В файле `bot/services/rag.py`:
- `similarity_top_k` - количество документов для поиска
- `num_queries` - количество вариантов запроса для поиска
- `temperature` - температура LLM (0.1 для детерминированности)

### Настройка эмбеддингов

В файле `bot/make_embeddings.py`:
- `CHUNK_SIZE = 1024` - размер чанка текста
- `CHUNK_OVERLAP = 256` - перекрытие между чанками

### Планировщик задач

В файле `bot/get_bot.py` настраиваются задачи по расписанию:
```python
scheduler.add_job(
    send_documents.send_message_on_time,
    'cron',
    hour=11,
    minute=30,
    timezone=timezone(timedelta(hours=+3))
)
```

## 🐛 Отладка

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Только бот
docker-compose logs -f bot-inner

# Последние 100 строк
docker-compose logs --tail=100 bot-inner
```

### Логи хранятся в

- Внутри контейнера: `/app/logs/`
- Через volume: `inner_logs`

### Пересоздание эмбеддингов

```bash
docker-compose down
docker volume rm chat_bot_inner_embeddings_inner
docker-compose -f embeddings.yml up --build
docker-compose up -d
```

## 📊 Мониторинг

Логи включают:
- Действия пользователей
- Запросы к LLM
- Ошибки и исключения
- Статистику использования команд

## 🔒 Безопасность

- Все API ключи хранятся в `.env` файлах (не в репозитории)
- Доступ к административным функциям по паролю
- Фильтры для ограничения доступа определенным группам пользователей
- Логирование всех действий


## 👥 Контакты

Для вопросов и поддержки обращайтесь к разработчикам через функцию обратной связи в боте.
