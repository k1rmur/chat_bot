# Чат-бот с RAG системой

## Описание

Интеллектуальный чат-бот для ВКонтакте, построенный на основе технологии RAG (Retrieval-Augmented Generation). Бот предназначен для ответов на вопросы пользователей на основе загруженной базы документов, включая PDF-файлы с законодательными актами, договорами и техническими документами.

## Основные возможности

- 🤖 **Интеллектуальные ответы**: Использует GigaChat для генерации ответов на основе контекста из документов
- 📄 **Обработка документов**: Поддержка PDF, DOCX и других форматов документов
- 🔍 **Продвинутый поиск**: Гибридный поиск, сочетающий векторный и BM25 алгоритмы
- 💬 **ВКонтакте интеграция**: Полная интеграция с VK API через VKBottle
- 🔄 **Обратная связь**: Встроенная система сбора отзывов от пользователей
- 📝 **Логирование**: Детальное логирование действий пользователей
- 🗃️ **База данных**: Хранение истории сообщений и рейтингов

## Технологический стек

### Основные технологии
- **Python 3.10** - основной язык программирования
- **VKBottle** - фреймворк для создания VK ботов
- **LlamaIndex** - фреймворк для RAG-систем
- **LangChain** - инструменты для работы с LLM
- **GigaChat** - языковая модель от Сбербанка
- **ChromaDB** - векторная база данных для эмбеддингов
- **SQLAlchemy** - ORM для работы с базой данных
- **Alembic** - миграции базы данных

### Машинное обучение и NLP
- **HuggingFace Transformers** - библиотека для работы с предобученными моделями
- **Sentence Transformers** - создание эмбеддингов предложений
- **PyTorch** - фреймворк машинного обучения
- **Multilingual E5** - многоязычная модель эмбеддингов

### Обработка документов
- **PDFMiner** - извлечение текста из PDF
- **python-docx** - работа с DOCX файлами
- **pdf2image** - конвертация PDF в изображения

### Инфраструктура
- **Docker & Docker Compose** - контейнеризация
- **APScheduler** - планировщик задач
- **aiosqlite** - асинхронная работа с SQLite

## Архитектура проекта

```
chat_bot/
├── bot/                          # Основной модуль бота
│   ├── config_data/             # Конфигурация
│   ├── data/                    # Документы для обучения
│   ├── data_citizens/           # Пользовательские документы
│   ├── database/                # Модели и запросы БД
│   ├── db_citizens/             # База данных эмбеддингов
│   ├── handlers/                # Обработчики сообщений
│   ├── keyboards/               # Клавиатуры VK
│   ├── lexicon/                 # Тексты и сообщения
│   ├── services/                # Бизнес-логика
│   ├── get_bot.py              # Точка входа бота
│   └── make_embeddings.py      # Создание эмбеддингов
├── alembic/                     # Миграции БД
├── documents/                   # Дополнительная документация
├── docker-compose.yml          # Конфигурация Docker
├── Dockerfile                  # Образ контейнера
├── requirements.txt            # Python зависимости
└── README.md                   # Документация
```

## Установка и настройка

### Предварительные требования

- Python 3.10+
- Docker и Docker Compose (для контейнерного запуска)
- Токен VK бота
- Аккаунт GigaChat API

### Локальная установка

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd chat_bot
```

2. **Создание виртуального окружения**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

3. **Установка зависимостей**
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

4. **Настройка переменных окружения**

Создайте файл `.env` в корневой директории:
```env
# VK Bot Configuration
BOT_TOKEN=your_vk_bot_token_here

# Database
DB_URL=sqlite+aiosqlite:///./bot_database.db

# GigaChat API
CREDENTIALS=your_gigachat_credentials_here

# Admin Users (comma-separated VK IDs)
SEND_MESSAGE_FROM=200830222,123456789

# Logging
LOG_LEVEL=INFO
```

5. **Инициализация базы данных**
```bash
alembic upgrade head
```

6. **Создание эмбеддингов**
```bash
cd bot
python make_embeddings.py
```

## Развертывание

### Docker (рекомендуемый способ)

1. **Подготовка окружения**
```bash
# Создайте .env файл с необходимыми переменными
cp .env.example .env
# Отредактируйте .env файл
```

2. **Сборка и запуск**
```bash
docker-compose up -d --build
```

3. **Проверка статуса**
```bash
docker-compose ps
docker-compose logs -f bot-outer
```

### Скрипты развертывания

Проект включает готовые скрипты для быстрого развертывания:

- `run_bots.sh` - запуск основного бота
- `run_bot_test.sh` - запуск в тестовом режиме
- `make_embeddings.sh` - создание эмбеддингов

```bash
# Запуск основного бота
chmod +x run_bots.sh
./run_bots.sh

# Создание эмбеддингов
chmod +x make_embeddings.sh
./make_embeddings.sh
```

## Конфигурация

### Основные настройки

1. **Настройка модели эмбеддингов** (`bot/make_embeddings.py`):
```python
embeddings = HuggingFaceEmbedding(
    model_name="intfloat/multilingual-e5-small", 
    device="cuda" if torch.cuda.is_available() else "cpu"
)
```

2. **Настройка RAG системы** (`bot/services/rag.py`):
```python
retriever = QueryFusionRetriever(
    [bm25_retriever, vector_retriever],
    similarity_top_k=30,
    num_queries=2,
    mode="dist_based_score"
)
```

3. **Настройка GigaChat**:
```python
llm = GigaChat(
    credentials=CREDENTIALS,
    scope="GIGACHAT_API_B2B",
    model="GigaChat-2-Pro",
    temperature=0.1,
    timeout=60
)
```

### Добавление документов

1. Поместите новые документы в папку `bot/data/` или `bot/data_citizens/`
2. Запустите создание эмбеддингов:
```bash
python bot/make_embeddings.py
```

## Использование

### Основные команды бота

- **Обратная связь** - отправка обратной связи разработчикам
- **Рейтинг ответов** - оценка качества полученных ответов
- **Вопросы по документам** - задавайте любые вопросы по загруженным документам

### Пример взаимодействия

```
Пользователь: Какие требования к договорам в водном кодексе?

Бот: На основе Водного кодекса Российской Федерации, требования 
к договорам включают:

1. Договор водопользования должен содержать...
2. Срок действия договора не может превышать...
3. В договоре указываются цели использования водного объекта...

```

## Мониторинг и логи

### Просмотр логов

```bash
# Docker
docker-compose logs -f bot-outer

# Локальный запуск
tail -f logs/bot.log
```

### Мониторинг базы данных

```bash
# Подключение к контейнеру
docker-compose exec bot-outer bash

# Просмотр статуса БД
sqlite3 bot_database.db ".tables"
```

## Обслуживание

### Обновление эмбеддингов

При добавлении новых документов:
```bash
# Остановка бота
docker-compose stop bot-outer

# Обновление эмбеддингов
docker-compose exec bot-outer python make_embeddings.py

# Перезапуск
docker-compose start bot-outer
```

### Резервное копирование

```bash
# Создание бэкапа БД
docker-compose exec bot-outer cp bot_database.db /backup/

# Бэкап эмбеддингов
docker cp container_name:/app/bot/db_citizens ./backup/embeddings/
```

## Разработка

### Добавление новых обработчиков

1. Создайте новый обработчик в `bot/handlers/`
2. Зарегистрируйте в `bot/handlers/__init__.py`
3. Добавьте соответствующие тексты в `bot/lexicon/`

### Тестирование

```bash
# Запуск в тестовом режиме
docker-compose -f docker-compose_test.yml up

# Локальное тестирование
python -m pytest tests/
```

## Устранение неполадок

### Частые проблемы

1. **Ошибка токена VK**: Проверьте правильность BOT_TOKEN в .env
2. **Проблемы с GigaChat**: Убедитесь в корректности CREDENTIALS
3. **Медленные ответы**: Проверьте доступность GPU для эмбеддингов
4. **Ошибки БД**: Выполните `alembic upgrade head`

### Диагностика

```bash
# Проверка состояния сервисов
docker-compose ps

# Просмотр ресурсов
docker stats

# Проверка сети
docker network ls
```